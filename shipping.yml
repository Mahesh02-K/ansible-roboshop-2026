- name: Configuring shipping component
  hosts: shipping
  become: yes
  tasks:
#   - name: Install maven and mysql
#     ansible.builtin.dnf:
#       name: "{ item }"
#       state: installed
#     loop:
#     - maven
#     - mysql

  - name: Install maven and mysql
    ansible.builtin.dnf:
      name: "{{ item }}"
      state: present
    loop:
      - maven
      - mysql